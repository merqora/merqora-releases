version: '3.8'

services:
  ai-support:
    build: .
    container_name: vinzay-ai-support
    ports:
      - "8000:8000"
    environment:
      - VINZAY_AI_API_HOST=0.0.0.0
      - VINZAY_AI_API_PORT=8000
      - VINZAY_AI_DEBUG=false
      - VINZAY_AI_SUPABASE_URL=${SUPABASE_URL}
      - VINZAY_AI_SUPABASE_KEY=${SUPABASE_SERVICE_KEY}
      - VINZAY_AI_CONFIDENCE_THRESHOLD=70
      - VINZAY_AI_SUPPORT_USER_ID=${SUPPORT_USER_ID}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - ./logs:/app/logs
    networks:
      - vinzay-network

  # Optional: Redis for caching (future enhancement)
  # redis:
  #   image: redis:7-alpine
  #   container_name: Vinzay-ai-redis
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - vinzay-network

networks:
  vinzay-network:
    driver: bridge
