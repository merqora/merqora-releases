version: '3.8'

services:
  ai-support:
    build: .
    container_name: mercora-ai-support
    ports:
      - "8000:8000"
    environment:
      - mercora_ai_API_HOST=0.0.0.0
      - mercora_ai_API_PORT=8000
      - mercora_ai_DEBUG=false
      - mercora_ai_SUPABASE_URL=${SUPABASE_URL}
      - mercora_ai_SUPABASE_KEY=${SUPABASE_SERVICE_KEY}
      - mercora_ai_CONFIDENCE_THRESHOLD=70
      - mercora_ai_SUPPORT_USER_ID=${SUPPORT_USER_ID}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - ./logs:/app/logs
    networks:
      - mercora-network

  # Optional: Redis for caching (future enhancement)
  # redis:
  #   image: redis:7-alpine
  #   container_name: mercora-ai-redis
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - mercora-network

networks:
  mercora-network:
    driver: bridge
