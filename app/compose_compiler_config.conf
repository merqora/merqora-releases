// Compose Compiler Stability Configuration
// Clases externas que el compilador no puede inferir como estables.
// Las listas del feed son efectivamente inmutables (siempre se emiten copias
// nuevas desde los StateFlow del ViewModel), asi los items son skippable.
kotlin.collections.List
kotlin.collections.Set
kotlin.collections.Map
java.time.*
java.util.Date
